export class Character {
  constructor(public name?: string,
              public surname?: string,
              public psi?: number,
              public psi_weight?: number,
              public iwa?: number,
              public iwa_weight?: number,
              public avg?: number) {
  }
}


// ? operator elvisa (safe navigation operator)
